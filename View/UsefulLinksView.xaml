<UserControl x:Class="HouseholdMS.View.UsefulLinksView" 
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignHeight="720" d:DesignWidth="980"
             SnapsToDevicePixels="True" UseLayoutRounding="True">

    <UserControl.Resources>
        <!-- Tarjeta contenedor (no clicable) -->
        <Style x:Key="CardContainer" TargetType="Border">
            <Setter Property="Padding" Value="14"/>
            <Setter Property="Margin" Value="8"/>
            <Setter Property="MinWidth" Value="320"/>
            <Setter Property="Background" Value="#FFFFFF"/>
            <Setter Property="BorderBrush" Value="#E3E8F2"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="CornerRadius" Value="12"/>
            <Setter Property="SnapsToDevicePixels" Value="True"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="BorderBrush" Value="#BFD3F2"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- Botón de recurso (abre sitio/archivo) -->
        <Style x:Key="ResourceButton" TargetType="Button">
            <Setter Property="Padding" Value="6,6"/>
            <Setter Property="Margin" Value="0,6,6,0"/>
            <Setter Property="MinWidth" Value="160"/>
            <Setter Property="HorizontalContentAlignment" Value="Left"/>
            <Setter Property="Background" Value="#FFFFFF"/>
            <Setter Property="BorderBrush" Value="#DCE3F1"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="8" Padding="{TemplateBinding Padding}">
                            <DockPanel LastChildFill="True">
                                <TextBlock Text="{Binding Icon}" Margin="0,0,8,0" VerticalAlignment="Center"/>
                                <StackPanel>
                                    <!-- Título legible -->
                                    <TextBlock Text="{Binding Label}" FontWeight="SemiBold" Foreground="#0B2546"/>
                                    <!-- Debajo: nombre real del archivo o dominio -->
                                    <TextBlock Text="{Binding ShortTarget}" FontSize="10" Foreground="#6B7280"/>
                                </StackPanel>
                            </DockPanel>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#F8FBFF"/>
                                <Setter Property="BorderBrush" Value="#BFD3F2"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="#EFF6FF"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Insignias + cabeceras de secciones -->
        <Style x:Key="BadgeStyle" TargetType="Border">
            <Setter Property="CornerRadius" Value="8"/>
            <Setter Property="Padding" Value="4,1"/>
            <Setter Property="Margin" Value="6,0,0,0"/>
            <Setter Property="Background" Value="#EEF2FF"/>
            <Setter Property="BorderBrush" Value="#C7D2FE"/>
            <Setter Property="BorderThickness" Value="1"/>
        </Style>

        <Style x:Key="SectionHeaderText" TargetType="TextBlock">
            <Setter Property="FontSize" Value="18"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Foreground" Value="#0B2546"/>
            <Setter Property="Margin" Value="4,0,0,4"/>
        </Style>
    </UserControl.Resources>

    <Grid Background="#F7F9FD">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Título -->
        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="8,4,8,4">
            <TextBlock Text="Enlaces útiles" FontSize="20" FontWeight="Bold" Foreground="#0A2A5E"/>
            <Border Style="{StaticResource BadgeStyle}">
                <TextBlock Text="{Binding TotalResourceCount, StringFormat=' {0} recursos'}" FontSize="11" Foreground="#374151"/>
            </Border>
        </StackPanel>

        <!-- Aviso -->
        <TextBlock Grid.Row="1" Margin="8,0,8,8" Foreground="#4B5563" FontSize="12" TextWrapping="Wrap">
            <Run FontWeight="Bold">Aviso:</Run> usa el traductor del navegador integrado si es necesario.
        </TextBlock>

        <!-- Listado -->
        <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto">
            <ItemsControl ItemsSource="{Binding Groups}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <StackPanel Margin="4,8,4,8">
                            <TextBlock Text="{Binding Header}" Style="{StaticResource SectionHeaderText}"/>
                            <ItemsControl ItemsSource="{Binding Items}">
                                <ItemsControl.ItemsPanel>
                                    <ItemsPanelTemplate>
                                        <WrapPanel/>
                                    </ItemsPanelTemplate>
                                </ItemsControl.ItemsPanel>
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <Border Style="{StaticResource CardContainer}">
                                            <Grid>
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="Auto"/>
                                                    <ColumnDefinition Width="*"/>
                                                </Grid.ColumnDefinitions>

                                                <!-- Icono -->
                                                <TextBlock Grid.Column="0"
                                                           Text="{Binding Icon}"
                                                           FontSize="22"
                                                           Margin="0,0,12,0"
                                                           VerticalAlignment="Top"/>

                                                <!-- Texto + botones de recursos -->
                                                <StackPanel Grid.Column="1">
                                                    <TextBlock Text="{Binding Title}" FontSize="14" FontWeight="Semibold" Foreground="#0B2546"/>
                                                    <TextBlock Text="{Binding Description}" Foreground="#4B5563" FontSize="12" TextWrapping="Wrap" Margin="0,4,0,8"/>

                                                    <WrapPanel>
                                                        <ItemsControl ItemsSource="{Binding Resources}">
                                                            <ItemsControl.ItemsPanel>
                                                                <ItemsPanelTemplate>
                                                                    <WrapPanel/>
                                                                </ItemsPanelTemplate>
                                                            </ItemsControl.ItemsPanel>
                                                            <ItemsControl.ItemTemplate>
                                                                <DataTemplate>
                                                                    <Button Style="{StaticResource ResourceButton}"
                                                                            Click="ResourceButton_Click"
                                                                            Tag="{Binding}"/>
                                                                </DataTemplate>
                                                            </ItemsControl.ItemTemplate>
                                                        </ItemsControl>
                                                    </WrapPanel>
                                                </StackPanel>
                                            </Grid>
                                        </Border>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                        </StackPanel>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>
    </Grid>
</UserControl>
