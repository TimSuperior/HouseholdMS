<UserControl x:Class="HouseholdMS.View.Dashboard.AddServiceControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignHeight="600"
             d:DesignWidth="440"
             Background="White">

    <Border Padding="25"
            CornerRadius="12"
            BorderBrush="#E0E0E0"
            BorderThickness="1"
            Background="White"
            SnapsToDevicePixels="True">

        <Grid>
            <!-- ===== SAME LOCAL PALETTE/STYLES AS AddHouseholdControl ===== -->
            <Grid.Resources>
                <!-- Palette -->
                <SolidColorBrush x:Key="AccentBrush"   Color="#1976D2"/>
                <SolidColorBrush x:Key="SuccessBrush"  Color="#4CAF50"/>
                <SolidColorBrush x:Key="DangerBrush"   Color="#C62828"/>
                <SolidColorBrush x:Key="MutedBrush"    Color="#9E9E9E"/>
                <SolidColorBrush x:Key="InputBorderBrush" Color="#C0C0C0"/>

                <!-- Header chips (kept for parity; not used here but harmless) -->
                <Style x:Key="ChipBorder" TargetType="Border">
                    <Setter Property="CornerRadius" Value="10"/>
                    <Setter Property="Padding" Value="8,3"/>
                    <Setter Property="Margin" Value="6,0,0,0"/>
                    <Setter Property="VerticalAlignment" Value="Center"/>
                    <Setter Property="SnapsToDevicePixels" Value="True"/>
                </Style>
                <Style x:Key="ChipText" TargetType="TextBlock">
                    <Setter Property="Foreground" Value="White"/>
                    <Setter Property="FontWeight" Value="SemiBold"/>
                </Style>
                <Style x:Key="IdChipText" TargetType="TextBlock">
                    <Setter Property="Foreground" Value="#37474F"/>
                    <Setter Property="FontWeight" Value="SemiBold"/>
                </Style>

                <!-- Labels -->
                <Style x:Key="FormLabel" TargetType="TextBlock">
                    <Setter Property="FontWeight" Value="SemiBold"/>
                    <Setter Property="Margin" Value="0,10,0,4"/>
                    <Setter Property="Foreground" Value="#2E3B4E"/>
                </Style>

                <!-- Inputs -->
                <Style x:Key="ErrorBoxStyle" TargetType="TextBox">
                    <Setter Property="Height" Value="32"/>
                    <Setter Property="Padding" Value="8,0"/>
                    <Setter Property="Background" Value="White"/>
                    <Setter Property="BorderBrush" Value="{StaticResource InputBorderBrush}"/>
                    <Setter Property="BorderThickness" Value="1"/>
                    <Style.Triggers>
                        <Trigger Property="IsKeyboardFocusWithin" Value="True">
                            <Setter Property="BorderBrush" Value="{StaticResource AccentBrush}"/>
                        </Trigger>
                        <Trigger Property="Tag" Value="error">
                            <Setter Property="BorderBrush" Value="Red"/>
                            <Setter Property="BorderThickness" Value="1.5"/>
                            <Setter Property="ToolTip" Value="This field is required."/>
                        </Trigger>
                    </Style.Triggers>
                </Style>
                <Style x:Key="InputTextBox" TargetType="TextBox" BasedOn="{StaticResource ErrorBoxStyle}" />

                <!-- Summary grid text styles -->
                <Style x:Key="SummaryLabelStyle" TargetType="TextBlock">
                    <Setter Property="FontWeight" Value="SemiBold"/>
                    <Setter Property="VerticalAlignment" Value="Center"/>
                    <Setter Property="Margin" Value="0,4,8,4"/>
                    <Setter Property="TextWrapping" Value="NoWrap"/>
                </Style>
                <Style x:Key="SummaryValueStyle" TargetType="TextBlock">
                    <Setter Property="VerticalAlignment" Value="Center"/>
                    <Setter Property="Margin" Value="0,4,0,4"/>
                    <Setter Property="TextWrapping" Value="Wrap"/>
                    <Setter Property="TextTrimming" Value="CharacterEllipsis"/>
                </Style>

                <!-- Rounded action buttons -->
                <Style x:Key="ActionButtonStyle" TargetType="Button">
                    <Setter Property="Height" Value="36"/>
                    <Setter Property="MinWidth" Value="100"/>
                    <Setter Property="Margin" Value="0,0,8,0"/>
                    <Setter Property="Padding" Value="10,4"/>
                    <Setter Property="FontWeight" Value="Bold"/>
                    <Setter Property="Foreground" Value="White"/>
                    <Setter Property="Cursor" Value="Hand"/>
                    <Setter Property="BorderThickness" Value="0"/>
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="Button">
                                <Border Background="{TemplateBinding Background}" CornerRadius="8">
                                    <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                </Border>
                                <ControlTemplate.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter Property="Opacity" Value="0.9"/>
                                    </Trigger>
                                    <Trigger Property="IsPressed" Value="True">
                                        <Setter Property="Opacity" Value="0.7"/>
                                    </Trigger>
                                    <Trigger Property="IsEnabled" Value="False">
                                        <Setter Property="Opacity" Value="0.55"/>
                                    </Trigger>
                                </ControlTemplate.Triggers>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                </Style>
                <Style x:Key="PrimaryButton" TargetType="Button" BasedOn="{StaticResource ActionButtonStyle}">
                    <Setter Property="Background" Value="{StaticResource SuccessBrush}"/>
                </Style>
                <Style x:Key="CancelButton" TargetType="Button" BasedOn="{StaticResource ActionButtonStyle}">
                    <Setter Property="Background" Value="{StaticResource DangerBrush}"/>
                </Style>
            </Grid.Resources>

            <!-- ===== Layout ===== -->
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Header -->
            <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,12">
                <TextBlock Text="🛠" FontSize="22" Margin="0,0,8,0"/>
                <TextBlock Text="Start Service Call"
                           FontSize="20"
                           FontWeight="Bold"
                           Foreground="#2E3B4E"/>
            </StackPanel>

            <!-- Body -->
            <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
                <StackPanel>

                    <!-- Household summary (read-only) -->
                    <Border BorderBrush="#EDEDED" BorderThickness="1" CornerRadius="8" Padding="12">
                        <Grid>
                            <!-- Explicit rows prevent overlap -->
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="150"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>

                            <TextBlock Grid.Row="0" Grid.Column="0" Text="Household ID:" Style="{StaticResource SummaryLabelStyle}"/>
                            <TextBlock x:Name="HHIdText" Grid.Row="0" Grid.Column="1" Style="{StaticResource SummaryValueStyle}"/>

                            <TextBlock Grid.Row="1" Grid.Column="0" Text="Owner:" Style="{StaticResource SummaryLabelStyle}"/>
                            <TextBlock x:Name="OwnerText" Grid.Row="1" Grid.Column="1" Style="{StaticResource SummaryValueStyle}"/>

                            <TextBlock Grid.Row="2" Grid.Column="0" Text="User:" Style="{StaticResource SummaryLabelStyle}"/>
                            <TextBlock x:Name="UserText" Grid.Row="2" Grid.Column="1" Style="{StaticResource SummaryValueStyle}"/>

                            <TextBlock Grid.Row="3" Grid.Column="0" Text="Municipality:" Style="{StaticResource SummaryLabelStyle}"/>
                            <TextBlock x:Name="MunicipalityText" Grid.Row="3" Grid.Column="1" Style="{StaticResource SummaryValueStyle}"/>

                            <TextBlock Grid.Row="4" Grid.Column="0" Text="District:" Style="{StaticResource SummaryLabelStyle}"/>
                            <TextBlock x:Name="DistrictText" Grid.Row="4" Grid.Column="1" Style="{StaticResource SummaryValueStyle}"/>

                            <TextBlock Grid.Row="5" Grid.Column="0" Text="Contact:" Style="{StaticResource SummaryLabelStyle}"/>
                            <TextBlock x:Name="ContactText" Grid.Row="5" Grid.Column="1" Style="{StaticResource SummaryValueStyle}"/>

                            <TextBlock Grid.Row="6" Grid.Column="0" Text="Status:" Style="{StaticResource SummaryLabelStyle}"/>
                            <TextBlock x:Name="StatusText" Grid.Row="6" Grid.Column="1" Style="{StaticResource SummaryValueStyle}"/>
                        </Grid>
                    </Border>

                    <!-- Space -->
                    <Border Height="10" Background="Transparent"/>

                    <!-- Problem -->
                    <TextBlock Text="Problem description (optional)" Style="{StaticResource FormLabel}" Margin="0,0,0,6"/>
                    <TextBox x:Name="ProblemBox"
                             Height="120"
                             AcceptsReturn="True"
                             TextWrapping="Wrap"
                             VerticalScrollBarVisibility="Auto"
                             Style="{StaticResource InputTextBox}"/>
                </StackPanel>
            </ScrollViewer>

            <!-- Footer -->
            <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,18,0,0">
                <Button x:Name="ConfirmBtn"
                        Content="✔ Confirm Service"
                        Style="{StaticResource PrimaryButton}"
                        Click="ConfirmBtn_Click"/>
                <Button x:Name="CancelBtn"
                        Content="✖ Cancel"
                        Style="{StaticResource CancelButton}"
                        Click="CancelBtn_Click"/>
            </StackPanel>
        </Grid>
    </Border>
</UserControl>
