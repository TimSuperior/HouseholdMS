<UserControl x:Class="HouseholdMS.View.Dashboard.AddServiceControl" 
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:res="clr-namespace:HouseholdMS.Resources"
             mc:Ignorable="d"
             d:DesignHeight="760"
             d:DesignWidth="980"
             MinHeight="720"
             MinWidth="960"
             Background="#F6F7FB"
             FontFamily="Segoe UI"
             FontSize="15"
             TextOptions.TextFormattingMode="Ideal"
             TextOptions.TextRenderingMode="ClearType"
             SnapsToDevicePixels="True"
             UseLayoutRounding="True">

    <Border Padding="28"
            CornerRadius="16"
            BorderBrush="#E5E7EB"
            BorderThickness="1"
            Background="White"
            SnapsToDevicePixels="True">
        <Border.Effect>
            <DropShadowEffect BlurRadius="18" ShadowDepth="2" Opacity="0.16" />
        </Border.Effect>

        <Grid>
            <!-- ===== PALETTE & STYLES ===== -->
            <Grid.Resources>
                <SolidColorBrush x:Key="AccentBrush"         Color="#2563EB"/>
                <SolidColorBrush x:Key="SuccessBrush"        Color="#16A34A"/>
                <SolidColorBrush x:Key="DangerBrush"         Color="#DC2626"/>
                <SolidColorBrush x:Key="MutedBrush"          Color="#64748B"/>
                <SolidColorBrush x:Key="InputBorderBrush"    Color="#CBD5E1"/>
                <SolidColorBrush x:Key="CardBgBrush"         Color="#FAFBFF"/>
                <SolidColorBrush x:Key="DividerBrush"        Color="#E5E7EB"/>
                <SolidColorBrush x:Key="LabelBrush"          Color="#334155"/>
                <SolidColorBrush x:Key="ValueBrush"          Color="#0F172A"/>

                <Style x:Key="HeaderTitle" TargetType="TextBlock">
                    <Setter Property="FontSize" Value="24"/>
                    <Setter Property="FontWeight" Value="Bold"/>
                    <Setter Property="Foreground" Value="#0F172A"/>
                    <Setter Property="VerticalAlignment" Value="Center"/>
                </Style>
                <Style x:Key="HeaderInfo" TargetType="TextBlock">
                    <Setter Property="FontSize" Value="14"/>
                    <Setter Property="Foreground" Value="{StaticResource MutedBrush}"/>
                    <Setter Property="TextWrapping" Value="Wrap"/>
                    <Setter Property="Margin" Value="0,6,0,0"/>
                </Style>

                <Style x:Key="FormLabel" TargetType="TextBlock">
                    <Setter Property="FontSize" Value="16"/>
                    <Setter Property="FontWeight" Value="SemiBold"/>
                    <Setter Property="Margin" Value="0,10,0,6"/>
                    <Setter Property="Foreground" Value="{StaticResource LabelBrush}"/>
                </Style>

                <Style x:Key="ErrorBoxStyle" TargetType="TextBox">
                    <Setter Property="Height" Value="160"/>
                    <Setter Property="Padding" Value="10,6"/>
                    <Setter Property="Background" Value="White"/>
                    <Setter Property="BorderBrush" Value="{StaticResource InputBorderBrush}"/>
                    <Setter Property="BorderThickness" Value="1"/>
                    <Setter Property="ScrollViewer.VerticalScrollBarVisibility" Value="Auto"/>
                    <Style.Triggers>
                        <Trigger Property="IsKeyboardFocusWithin" Value="True">
                            <Setter Property="BorderBrush" Value="{StaticResource AccentBrush}"/>
                        </Trigger>
                        <Trigger Property="Tag" Value="error">
                            <Setter Property="BorderBrush" Value="Red"/>
                            <Setter Property="BorderThickness" Value="1.5"/>
                            <Setter Property="ToolTip" Value="{x:Static res:Strings.AS_FieldRequired}"/>
                        </Trigger>
                    </Style.Triggers>
                </Style>
                <Style x:Key="InputTextBox" TargetType="TextBox" BasedOn="{StaticResource ErrorBoxStyle}" />

                <Style x:Key="SummaryLabelStyle" TargetType="TextBlock">
                    <Setter Property="FontSize" Value="15"/>
                    <Setter Property="Foreground" Value="{StaticResource LabelBrush}"/>
                    <Setter Property="FontWeight" Value="SemiBold"/>
                    <Setter Property="VerticalAlignment" Value="Center"/>
                    <Setter Property="Margin" Value="0,8,12,8"/>
                    <Setter Property="TextWrapping" Value="NoWrap"/>
                </Style>
                <Style x:Key="SummaryValueStyle" TargetType="TextBlock">
                    <Setter Property="FontSize" Value="15"/>
                    <Setter Property="Foreground" Value="{StaticResource ValueBrush}"/>
                    <Setter Property="VerticalAlignment" Value="Center"/>
                    <Setter Property="Margin" Value="0,8,0,8"/>
                    <Setter Property="TextWrapping" Value="Wrap"/>
                    <Setter Property="TextTrimming" Value="CharacterEllipsis"/>
                </Style>

                <Style x:Key="ActionButtonStyle" TargetType="Button">
                    <Setter Property="Height" Value="42"/>
                    <Setter Property="MinWidth" Value="120"/>
                    <Setter Property="Margin" Value="0,0,10,0"/>
                    <Setter Property="Padding" Value="14,8"/>
                    <Setter Property="FontWeight" Value="Bold"/>
                    <Setter Property="Foreground" Value="White"/>
                    <Setter Property="Cursor" Value="Hand"/>
                    <Setter Property="BorderThickness" Value="0"/>
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="Button">
                                <Border Background="{TemplateBinding Background}" CornerRadius="10">
                                    <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                </Border>
                                <ControlTemplate.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter Property="Opacity" Value="0.92"/>
                                    </Trigger>
                                    <Trigger Property="IsPressed" Value="True">
                                        <Setter Property="Opacity" Value="0.75"/>
                                    </Trigger>
                                    <Trigger Property="IsEnabled" Value="False">
                                        <Setter Property="Opacity" Value="0.55"/>
                                    </Trigger>
                                </ControlTemplate.Triggers>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                </Style>
                <Style x:Key="PrimaryButton" TargetType="Button" BasedOn="{StaticResource ActionButtonStyle}">
                    <Setter Property="Background" Value="{StaticResource SuccessBrush}"/>
                </Style>
                <Style x:Key="CancelButton" TargetType="Button" BasedOn="{StaticResource ActionButtonStyle}">
                    <Setter Property="Background" Value="{StaticResource DangerBrush}"/>
                </Style>
            </Grid.Resources>

            <!-- ===== LAYOUT ===== -->
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Header -->
            <StackPanel Grid.Row="0" Orientation="Vertical" Margin="0,0,0,14">
                <DockPanel LastChildFill="False">
                    <TextBlock Text="🛠" FontSize="26" Margin="0,0,10,0" VerticalAlignment="Center"/>
                    <TextBlock Text="{x:Static res:Strings.AS_Header}" Style="{StaticResource HeaderTitle}"/>
                </DockPanel>

                <!-- Filled by code-behind SetInfoText(...) -->
                <TextBlock x:Name="ConfirmInfoText"
                           Style="{StaticResource HeaderInfo}"
                           Text=""
                           Visibility="Visible"/>
            </StackPanel>

            <!-- Body -->
            <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
                <StackPanel>

                    <!-- Household summary card -->
                    <Border BorderBrush="{StaticResource DividerBrush}"
                            BorderThickness="1"
                            CornerRadius="12"
                            Background="{StaticResource CardBgBrush}"
                            Padding="16">
                        <StackPanel>
                            <TextBlock Text="{x:Static res:Strings.AS_Summary_Title}" Style="{StaticResource FormLabel}" Margin="0,0,0,10"/>
                            <Grid>
                                <!-- Explicit rows: content/divider pairs to prevent overlap -->
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="1"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="1"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="1"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="1"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="1"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="1"/>
                                    <RowDefinition Height="Auto"/>
                                </Grid.RowDefinitions>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="220"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>

                                <!-- Row 0 -->
                                <TextBlock Grid.Row="0" Grid.Column="0" Text="{x:Static res:Strings.AS_Summary_HouseholdID}" Style="{StaticResource SummaryLabelStyle}"/>
                                <TextBlock x:Name="HHIdText" Grid.Row="0" Grid.Column="1" Style="{StaticResource SummaryValueStyle}"/>

                                <Border Grid.Row="1" Grid.ColumnSpan="2" Background="{StaticResource DividerBrush}" />

                                <!-- Row 2 -->
                                <TextBlock Grid.Row="2" Grid.Column="0" Text="{x:Static res:Strings.AS_Summary_Owner}" Style="{StaticResource SummaryLabelStyle}"/>
                                <TextBlock x:Name="OwnerText" Grid.Row="2" Grid.Column="1" Style="{StaticResource SummaryValueStyle}"/>

                                <Border Grid.Row="3" Grid.ColumnSpan="2" Background="{StaticResource DividerBrush}" />

                                <!-- Row 4 (label changed to literal "DNI:" to avoid resource dependency) -->
                                <TextBlock Grid.Row="4" Grid.Column="0" Text="DNI:" Style="{StaticResource SummaryLabelStyle}"/>
                                <TextBlock x:Name="UserText" Grid.Row="4" Grid.Column="1" Style="{StaticResource SummaryValueStyle}"/>

                                <Border Grid.Row="5" Grid.ColumnSpan="2" Background="{StaticResource DividerBrush}" />

                                <!-- Row 6 -->
                                <TextBlock Grid.Row="6" Grid.Column="0" Text="{x:Static res:Strings.AS_Summary_Municipality}" Style="{StaticResource SummaryLabelStyle}"/>
                                <TextBlock x:Name="MunicipalityText" Grid.Row="6" Grid.Column="1" Style="{StaticResource SummaryValueStyle}"/>

                                <Border Grid.Row="7" Grid.ColumnSpan="2" Background="{StaticResource DividerBrush}" />

                                <!-- Row 8 -->
                                <TextBlock Grid.Row="8" Grid.Column="0" Text="{x:Static res:Strings.AS_Summary_District}" Style="{StaticResource SummaryLabelStyle}"/>
                                <TextBlock x:Name="DistrictText" Grid.Row="8" Grid.Column="1" Style="{StaticResource SummaryValueStyle}"/>

                                <Border Grid.Row="9" Grid.ColumnSpan="2" Background="{StaticResource DividerBrush}" />

                                <!-- Row 10 -->
                                <TextBlock Grid.Row="10" Grid.Column="0" Text="{x:Static res:Strings.AS_Summary_Contact}" Style="{StaticResource SummaryLabelStyle}"/>
                                <TextBlock x:Name="ContactText" Grid.Row="10" Grid.Column="1" Style="{StaticResource SummaryValueStyle}"/>

                                <Border Grid.Row="11" Grid.ColumnSpan="2" Background="{StaticResource DividerBrush}" />

                                <!-- Row 12 -->
                                <TextBlock Grid.Row="12" Grid.Column="0" Text="{x:Static res:Strings.AS_Summary_Status}" Style="{StaticResource SummaryLabelStyle}"/>
                                <TextBlock x:Name="StatusText" Grid.Row="12" Grid.Column="1" Style="{StaticResource SummaryValueStyle}"/>
                            </Grid>
                        </StackPanel>
                    </Border>

                    <Border Height="14" Background="Transparent"/>

                    <!-- Problem -->
                    <TextBlock Text="{x:Static res:Strings.AS_Problem_Label}" Style="{StaticResource FormLabel}" Margin="0,0,0,6"/>
                    <TextBox x:Name="ProblemBox"
                             Style="{StaticResource InputTextBox}"
                             AcceptsReturn="True"
                             TextWrapping="Wrap"/>
                </StackPanel>
            </ScrollViewer>

            <!-- Footer: buttons pinned to the RIGHT -->
            <Grid Grid.Row="2" Margin="0,18,0,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="1" Orientation="Horizontal">
                    <Button x:Name="ConfirmBtn"
                            Content="{x:Static res:Strings.AS_Confirm}"
                            Style="{StaticResource PrimaryButton}"
                            Click="ConfirmBtn_Click"/>
                    <Button x:Name="CancelBtn"
                            Content="{x:Static res:Strings.AS_Cancel}"
                            Style="{StaticResource CancelButton}"
                            Click="CancelBtn_Click"/>
                </StackPanel>
            </Grid>
        </Grid>
    </Border>
</UserControl>
