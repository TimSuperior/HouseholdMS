<!-- View/EqTesting/SwitchTestMenuView.xaml -->
<UserControl x:Class="HouseholdMS.View.EqTesting.SwitchTestMenuView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="520" d:DesignWidth="900"
             MinWidth="720" MinHeight="480"
             Focusable="True"
             PreviewKeyDown="Root_PreviewKeyDown"
             KeyDown="Root_KeyDown">

    <UserControl.Resources>
        <!-- Navigation button -->
        <Style x:Key="NavButtonStyle" TargetType="Button">
            <Setter Property="Padding" Value="10,8"/>
            <Setter Property="MinWidth" Value="96"/>
            <Setter Property="Margin" Value="0,0,8,0"/>
            <!-- remove dotted focus cue -->
            <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="bd"
                                Background="#111827"
                                CornerRadius="10"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"
                                              RecognizesAccessKey="True"
                                              TextElement.Foreground="White"
                                              TextElement.FontWeight="SemiBold"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="bd" Property="Background" Value="#1F2937"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="bd" Property="Background" Value="#374151"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="bd" Property="Background" Value="#9CA3AF"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <!-- All white background -->
    <Grid Background="#FFFFFFFF"
          KeyboardNavigation.TabNavigation="None"
          KeyboardNavigation.ControlTabNavigation="None"
          KeyboardNavigation.DirectionalNavigation="None">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <!-- Header -->
            <RowDefinition Height="*"/>
            <!-- Content -->
            <RowDefinition Height="Auto"/>
            <!-- Footer -->
        </Grid.RowDefinitions>

        <!-- HEADER -->
        <Border x:Name="HeaderBar"
                Grid.Row="0"
                Background="#F7F9FC" BorderBrush="#E5E7EB" BorderThickness="0,0,0,1" Padding="12">
            <DockPanel LastChildFill="True">
                <StackPanel Orientation="Vertical">
                    <TextBlock x:Name="HeaderTitle"   FontSize="18" FontWeight="SemiBold"/>
                    <TextBlock x:Name="HeaderVersion" FontSize="12" Foreground="#6B7280"/>
                </StackPanel>
                <TextBlock x:Name="HeaderStep"
                           DockPanel.Dock="Right"
                           VerticalAlignment="Center"
                           Foreground="#6B7280" Margin="20,0,0,0"/>
            </DockPanel>
        </Border>

        <!-- MAIN (Image viewer only) -->
        <ScrollViewer x:Name="ImageScroll"
                      Grid.Row="1"
                      Padding="0"
                      VerticalScrollBarVisibility="Disabled"
                      HorizontalScrollBarVisibility="Disabled"
                      Background="#FFFFFFFF"
                      Focusable="False">
            <Grid Background="#FFFFFFFF">
                <Viewbox x:Name="ImageViewbox"
                         Stretch="Uniform"
                         StretchDirection="Both"
                         HorizontalAlignment="Stretch"
                         VerticalAlignment="Stretch"
                         SnapsToDevicePixels="True"
                         UseLayoutRounding="True">
                    <Image x:Name="PageImage"
                           Stretch="Uniform"
                           HorizontalAlignment="Stretch"
                           VerticalAlignment="Stretch"
                           SnapsToDevicePixels="True"
                           RenderOptions.BitmapScalingMode="Fant"/>
                </Viewbox>
            </Grid>
        </ScrollViewer>

        <!-- FOOTER -->
        <Border x:Name="FooterBar"
                Grid.Row="2"
                Background="#F7F9FC" BorderBrush="#E5E7EB" BorderThickness="0,1,0,0" Padding="10">
            <DockPanel>
                <StackPanel DockPanel.Dock="Right" Orientation="Horizontal">
                    <Button x:Name="PrevBtn" Content="◀ Back" Style="{StaticResource NavButtonStyle}"
                            Click="PrevBtn_Click" Focusable="False"/>
                    <Button x:Name="NextBtn" Content="Next ▶" Style="{StaticResource NavButtonStyle}"
                            Margin="0" Click="NextBtn_Click" Focusable="False"/>
                </StackPanel>
            </DockPanel>
        </Border>
    </Grid>
</UserControl>
