<UserControl x:Class="HouseholdMS.View.EqTesting.TemplateView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="520" d:DesignWidth="900"
             MinWidth="720" MinHeight="480">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <!-- Header -->
            <RowDefinition Height="*"/>
            <!-- Content -->
            <RowDefinition Height="Auto"/>
            <!-- Footer -->
        </Grid.RowDefinitions>

        <!-- HEADER -->
        <Border Grid.Row="0" Background="#F7F9FC" BorderBrush="#E5E7EB" BorderThickness="0,0,0,1" Padding="12">
            <DockPanel LastChildFill="True">
                <StackPanel Orientation="Vertical">
                    <TextBlock x:Name="HeaderTitle"   FontSize="18" FontWeight="SemiBold"/>
                    <TextBlock x:Name="HeaderVersion" FontSize="12" Foreground="#6B7280"/>
                </StackPanel>
                <TextBlock x:Name="HeaderStep"
                   DockPanel.Dock="Right"
                   VerticalAlignment="Center"
                   Foreground="#6B7280"/>
            </DockPanel>
        </Border>

        <!-- MAIN -->
        <ScrollViewer Grid.Row="1" Padding="16" VerticalScrollBarVisibility="Auto">
            <StackPanel>
                <TextBlock x:Name="PageTitle" FontSize="20" FontWeight="Bold" Margin="0,0,0,8"/>
                <Image x:Name="PageImage" Stretch="Uniform" MaxHeight="420" Margin="0,0,0,12"/>
                <StackPanel x:Name="StepsHost"/>
            </StackPanel>
        </ScrollViewer>

        <!-- FOOTER -->
        <Border Grid.Row="2" Background="#F7F9FC" BorderBrush="#E5E7EB" BorderThickness="0,1,0,0" Padding="10">
            <DockPanel>
                <TextBlock x:Name="StatusText" VerticalAlignment="Center" Opacity="0.8"/>
                <StackPanel DockPanel.Dock="Right" Orientation="Horizontal">
                    <Button x:Name="PrevBtn"   Content="Prev"   Width="96" Margin="0,0,8,0" Click="PrevBtn_Click"/>
                    <Button x:Name="NextBtn"   Content="Next"   Width="96" Margin="0,0,8,0" Click="NextBtn_Click"/>
                    <Button x:Name="FinishBtn" Content="Finish" Width="96" Click="FinishBtn_Click" Visibility="Collapsed"/>
                </StackPanel>
            </DockPanel>
        </Border>
    </Grid>
</UserControl>
