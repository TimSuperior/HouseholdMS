<UserControl x:Class="HouseholdMS.View.HouseholdsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:HouseholdMS.View"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <!-- Households Tab -->
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="10*"/>
            <RowDefinition Height="90*"/>

        </Grid.RowDefinitions>

        <Button Content="➕ Add Household" Click="AddHouseholdButton_Click" Command="{Binding AddHouseholdCommand}" Width="180" Margin="10,0,10,10"/>
        
        <StackPanel Grid.Row="1">


            <ListView x:Name="HouseholdListView" ItemsSource="{Binding Households}" Height="300">
                <ListView.View>
                    <GridView>
                        <GridViewColumn Header="HouseholdID" DisplayMemberBinding="{Binding HouseholdID}" Width="150"/>
                        <GridViewColumn Header="Owner Name" DisplayMemberBinding="{Binding OwnerName}" Width="150"/>
                        <GridViewColumn Header="Address" DisplayMemberBinding="{Binding Address}" Width="200"/>
                        <GridViewColumn Header="Contact" DisplayMemberBinding="{Binding ContactNum}" Width="100"/>
                        <GridViewColumn Header="Installation Date" DisplayMemberBinding="{Binding InstDate}" Width="100"/>
                        <GridViewColumn Header="Last Inspection" DisplayMemberBinding="{Binding LastInspDate}" Width="100"/>
                    </GridView>
                </ListView.View>
            </ListView>

           <!-- <TextBox x:Name="OwnerNameBox" Text="Owner Name" Foreground="Gray" Margin="0,0,0,5" GotFocus="ClearText" LostFocus="ResetText"/>
            <TextBox x:Name="AddressBox" Text="AddressBox" Foreground="Gray" Margin="0,0,0,5" GotFocus="ClearText" LostFocus="ResetText"/>
            <TextBox x:Name="ContactBox" Text="ContactBox" Foreground="Gray" Margin="0,0,0,5" GotFocus="ClearText" LostFocus="ResetText"/>
            <TextBox x:Name="InstDateBox" Text="InstDateBox" Foreground="Gray" Margin="0,0,0,5" GotFocus="ClearText" LostFocus="ResetText"/>
            <TextBox x:Name="LastInspBox" Text="LastInspBox" Foreground="Gray" Margin="0,0,0,5" GotFocus="ClearText" LostFocus="ResetText"/>
            -->
            
        </StackPanel>
    </Grid>
</UserControl>
