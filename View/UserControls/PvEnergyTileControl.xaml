<UserControl x:Class="HouseholdMS.View.UserControls.PvEnergyTileControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             MinHeight="130" MinWidth="240">
    <UserControl.Resources>
        <SolidColorBrush x:Key="Col.Heading" Color="#0F172A"/>
        <SolidColorBrush x:Key="Col.Muted"   Color="#667085"/>
        <SolidColorBrush x:Key="Col.Primary" Color="#2563EB"/>

        <Style x:Key="MutedText" TargetType="TextBlock">
            <Setter Property="Foreground" Value="{StaticResource Col.Muted}"/>
        </Style>

        <!-- Icon-only tiny button -->
        <Style x:Key="IconButton" TargetType="Button">
            <Setter Property="Width" Value="28"/>
            <Setter Property="Height" Value="28"/>
            <Setter Property="Padding" Value="0"/>
            <Setter Property="Background" Value="#00000000"/>
            <Setter Property="BorderBrush" Value="#D1D6E5"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="bd" CornerRadius="999"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="bd" Property="Background" Value="#0F2563EB"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="bd" Property="Background" Value="#1A2563EB"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="bd" Property="Opacity" Value="0.6"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Spin when StatusText == (loading...) -->
        <Style x:Key="RefreshGlyphSpinStyle" TargetType="TextBlock">
            <Setter Property="RenderTransform">
                <Setter.Value>
                    <RotateTransform Angle="0"/>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <DataTrigger Binding="{Binding StatusText, RelativeSource={RelativeSource AncestorType=UserControl}}" Value="(loading...)">
                    <DataTrigger.EnterActions>
                        <BeginStoryboard x:Name="SpinSB">
                            <Storyboard>
                                <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(RotateTransform.Angle)"
                                                 By="360" Duration="0:0:1" RepeatBehavior="Forever"/>
                            </Storyboard>
                        </BeginStoryboard>
                    </DataTrigger.EnterActions>
                    <DataTrigger.ExitActions>
                        <StopStoryboard BeginStoryboardName="SpinSB"/>
                    </DataTrigger.ExitActions>
                </DataTrigger>
            </Style.Triggers>
        </Style>
    </UserControl.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header (icon + title + status + tiny refresh) -->
        <Grid Grid.Row="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                <TextBlock Text="⚡" FontSize="18" Foreground="{StaticResource Col.Primary}" VerticalAlignment="Center"/>
                <TextBlock Text="PV Energy (PVGIS)" FontWeight="Bold" FontSize="16"
                           Foreground="{StaticResource Col.Heading}" Margin="8,0,0,0"/>
                <TextBlock Text="{Binding StatusText, RelativeSource={RelativeSource AncestorType=UserControl}}"
                           Margin="10,0,0,0" FontSize="12" Foreground="{StaticResource Col.Muted}"/>
            </StackPanel>

            <Button Grid.Column="1" Style="{StaticResource IconButton}" ToolTip="Refresh"
                    Click="Refresh_Click">
                <TextBlock Style="{StaticResource RefreshGlyphSpinStyle}"
                           Text="↻" FontSize="14" Foreground="{StaticResource Col.Primary}"/>
            </Button>
        </Grid>

        <!-- Main metrics -->
        <StackPanel Grid.Row="1" Margin="0,10,0,0">
            <TextBlock Text="{Binding MonthlyText, RelativeSource={RelativeSource AncestorType=UserControl}}"
                       FontSize="24" FontWeight="SemiBold" Foreground="{StaticResource Col.Heading}"/>
            <TextBlock Text="{Binding AnnualText, RelativeSource={RelativeSource AncestorType=UserControl}}"
                       Margin="0,6,0,0" Style="{StaticResource MutedText}"/>
        </StackPanel>

        <!-- Config / meta -->
        <TextBlock Grid.Row="2"
                   Text="{Binding ConfigText, RelativeSource={RelativeSource AncestorType=UserControl}}"
                   Foreground="{StaticResource Col.Muted}"
                   FontSize="11" Margin="0,8,0,0"/>
    </Grid>
</UserControl>
