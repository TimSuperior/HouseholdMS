<UserControl x:Class="HouseholdMS.View.Dashboard.DashboardView"  
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:ctrls="clr-namespace:HouseholdMS.View.UserControls"
             xmlns:local="clr-namespace:HouseholdMS.View.Dashboard">

    <UserControl.Resources>

        <!-- Base tile look -->
        <Style x:Key="TileButton" TargetType="Button">
            <Setter Property="Margin" Value="8"/>
            <Setter Property="Padding" Value="16"/>
            <Setter Property="MinHeight" Value="130"/>
            <Setter Property="MinWidth" Value="240"/>
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
            <Setter Property="VerticalContentAlignment" Value="Stretch"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="bd"
                                Background="{TemplateBinding Background}"
                                CornerRadius="16"
                                BorderBrush="#DDDDDD"
                                BorderThickness="1"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="bd" Property="BorderBrush" Value="#B0B0B0"/>
                                <Setter TargetName="bd" Property="Effect">
                                    <Setter.Value>
                                        <DropShadowEffect BlurRadius="8" ShadowDepth="1" Opacity="0.25"/>
                                    </Setter.Value>
                                </Setter>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="bd" Property="Background" Value="#F5F5F5"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="bd" Property="Opacity" Value="0.6"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Inventory tile = TileButton + stock highlight -->
        <Style x:Key="InventoryTileButtonStyle" TargetType="Button" BasedOn="{StaticResource TileButton}">
            <Style.Triggers>
                <DataTrigger Binding="{Binding IsZero}" Value="True">
                    <Setter Property="Background" Value="#FFEBEE"/>
                    <Setter Property="BorderBrush" Value="#E57373"/>
                    <Setter Property="BorderThickness" Value="2"/>
                </DataTrigger>
                <DataTrigger Binding="{Binding IsBelowThreshold}" Value="True">
                    <Setter Property="Background" Value="#FFF8E1"/>
                    <Setter Property="BorderBrush" Value="#FFB300"/>
                    <Setter Property="BorderThickness" Value="2"/>
                </DataTrigger>
                <DataTrigger Binding="{Binding IsOk}" Value="True">
                    <Setter Property="Background" Value="#E8F5E9"/>
                    <Setter Property="BorderBrush" Value="#81C784"/>
                    <Setter Property="BorderThickness" Value="1.5"/>
                </DataTrigger>
            </Style.Triggers>
        </Style>

        <!-- Keyed template (no DataType) -->
        <DataTemplate x:Key="InventoryTileTemplate">
            <Button Style="{StaticResource InventoryTileButtonStyle}" Click="InventoryTile_Click">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <TextBlock Text="{Binding Name}" FontWeight="Bold" FontSize="16"/>

                    <StackPanel Grid.Row="1" Margin="0,6,0,0">
                        <TextBlock Text="Remaining" Foreground="#666" FontSize="12"/>
                        <TextBlock Text="{Binding Remaining}" FontSize="30" FontWeight="SemiBold"/>
                    </StackPanel>

                    <StackPanel Grid.Row="2" Orientation="Horizontal" Margin="0,6,0,0">
                        <TextBlock Text="{Binding LowStockThreshold, StringFormat=Threshold: {0}}" Margin="0,0,12,0"/>
                        <TextBlock Text="{Binding LastRestockedDate, StringFormat=Restocked: {0:yyyy-MM-dd}}"/>
                    </StackPanel>
                </Grid>
            </Button>
        </DataTemplate>

    </UserControl.Resources>

    <Grid Background="#F0F2F5">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <DockPanel Grid.Row="0" Margin="12,8">
            <TextBlock Text="Dashboard" FontSize="22" FontWeight="Bold" VerticalAlignment="Center"/>
        </DockPanel>

        <!-- Row 1: Household (title + 3 tiles) -->
        <StackPanel Grid.Row="1" Margin="12,0,12,4">
            <DockPanel LastChildFill="True" Margin="0,0,0,6">
                <TextBlock Text="🏠 Household" FontSize="18" FontWeight="Bold" VerticalAlignment="Center" />
            </DockPanel>

            <UniformGrid Columns="3">
                <!-- Tile 1: Total households -->
                <Button Style="{StaticResource TileButton}" Click="OpenAllHouseholds_Click">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>

                        <StackPanel>
                            <TextBlock Text="Total Households" FontWeight="Bold"/>
                            <TextBlock x:Name="TotalCountText" Text="0" FontSize="34" Margin="0,4,0,0"/>
                        </StackPanel>
                    </Grid>
                </Button>

                <!-- Tile 2: Operational with ring -->
                <Button Style="{StaticResource TileButton}" Click="OpenOperational_Click">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>

                        <StackPanel>
                            <TextBlock Text="Operational" FontWeight="Bold"/>
                            <TextBlock x:Name="OperationalCountText" Text="0" FontSize="30" Margin="0,4,0,0"/>
                        </StackPanel>

                        <ctrls:CircularProgressBar x:Name="OperationalProgress"
                                                   Width="72" Height="72"
                                                   StrokeThickness="8"
                                                   ProgressBrush="#4CAF50"
                                                   TrackBrush="#E6E6E6"
                                                   Grid.Column="1"
                                                   HorizontalAlignment="Right"
                                                   VerticalAlignment="Center"/>
                    </Grid>
                </Button>

                <!-- Tile 3: Out of Service (DB: In Service) with ring -->
                <Button Style="{StaticResource TileButton}" Click="OpenOutOfService_Click">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>

                        <StackPanel>
                            <TextBlock Text="Out of Service" FontWeight="Bold"/>
                            <TextBlock x:Name="OutOfServiceCountText" Text="0" FontSize="30" Margin="0,4,0,0"/>
                        </StackPanel>

                        <ctrls:CircularProgressBar x:Name="OutOfServiceProgress"
                                                   Width="72" Height="72"
                                                   StrokeThickness="8"
                                                   ProgressBrush="#F44336"
                                                   TrackBrush="#E6E6E6"
                                                   Grid.Column="1"
                                                   HorizontalAlignment="Right"
                                                   VerticalAlignment="Center"/>
                    </Grid>
                </Button>
            </UniformGrid>
        </StackPanel>

        <!-- Row 2: Inventory -->
        <StackPanel Grid.Row="2" Margin="12,8,12,0">
            <DockPanel LastChildFill="True" Margin="0,0,0,6">
                <TextBlock Text="📦 Inventory" FontSize="18" FontWeight="Bold" VerticalAlignment="Center" />
                <Button Content="Open List"
                        Padding="8,4"
                        Margin="8,0,0,0"
                        HorizontalAlignment="Right"
                        DockPanel.Dock="Right"
                        Click="OpenInventoryList_Click"/>
            </DockPanel>

            <Border CornerRadius="16" Background="#FFFFFF" BorderBrush="#DDDDDD" BorderThickness="1">
                <ScrollViewer VerticalScrollBarVisibility="Auto">
                    <ItemsControl x:Name="InventoryItemsControl"
                                  ItemsSource="{Binding InventoryTiles}"
                                  ItemTemplate="{StaticResource InventoryTileTemplate}">
                        <ItemsControl.ItemsPanel>
                            <ItemsPanelTemplate>
                                <WrapPanel/>
                            </ItemsPanelTemplate>
                        </ItemsControl.ItemsPanel>
                        <ItemsControl.Margin>
                            <Thickness>4</Thickness>
                        </ItemsControl.Margin>
                    </ItemsControl>
                </ScrollViewer>
            </Border>
        </StackPanel>

        <!-- Row 3: Placeholder -->
        <Grid Grid.Row="3" Margin="12,12,12,12">
            <Grid.ColumnDefinitions>
                <ColumnDefinition/>
                <ColumnDefinition/>
            </Grid.ColumnDefinitions>
            <Border CornerRadius="16" Background="#FFFFFF" BorderBrush="#EEEEEE" BorderThickness="1" Margin="0,0,6,0"/>
            <Border Grid.Column="1" CornerRadius="16" Background="#FFFFFF" BorderBrush="#EEEEEE" BorderThickness="1" Margin="6,0,0,0"/>
            <TextBlock Text="(Reserved for future tiles…)" Foreground="#888" HorizontalAlignment="Center" VerticalAlignment="Center"/>
        </Grid>

    </Grid>
</UserControl>
